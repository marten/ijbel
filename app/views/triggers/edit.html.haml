%h1 Editing trigger

- semantic_form_for(@trigger) do |f|
  = f.error_messages
  = f.inputs
  - f.semantic_fields_for :responses do |response|
    = response.inputs
  = f.buttons

= link_to 'Show', @trigger
= link_to 'Back', triggers_path
